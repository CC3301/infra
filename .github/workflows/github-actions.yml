name: Docker Compose Deploy
on: [push]
jobs:
  Deploy-Monitoring-And-Ingress-PRD-Stack:
    runs-on: self-hosted
    steps:
      #- uses: actions/checkout@v2
      #- uses: sulthonzh/docker-remote-deployment-action@v0.1.2-beta
      - uses: sagebind/docker-swarm-deploy-action@v2
        with:
          remote_host: ssh://fink-home-docker-deploy@prd-docker0-kch.fink.home
          ssh_private_key: ${{ secrets.NEW_DOCKER_SSH_KEY }}
          ssh_public_key: ecdsa-sha2-nistp256 AAAAE2VjZHNhLXNoYTItbmlzdHAyNTYAAAAIbmlzdHAyNTYAAABBBNw0oDx9/Do0r8p6v9KEoqpEuxD0qFOA1sB3LYnIIDBI3JWDpZYGstJWCvZUZQGwf0ycD4IRBVoV3sKrtSnauus=
          #deployment_mode: docker-swarm
          #copy_stack_file: true
          #stack_file_name: monitoring-and-ingress-prd/docker-compose.yml
          #keep_files: 3
          args: stack deploy --compose-file monitoring-and-ingress-prd/docker-compose.yml monitoring-and-ingress-prd
          #args: monitoring-and-ingress-prd
          #deploy_path: /home/fink-home-docker-deploy/deploy
          #docker_registry_username: ffink85
          #docker_registry_password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

  Deploy-Teamspeak3-Server-PRD-Stack:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - uses: chaplyk/docker-compose-remote-action@v1.1
        with:
          ssh_host: 10.0.0.107:22
          ssh_user: fink-home-docker-deploy
          ssh_key: ${{ secrets.NEW_DOCKER_SSH_KEY }}
          compose_file: teamspeak3-server-prd/docker-compose.yml
          pull: true
          build: true

  Deploy-AdGuard-DNS-PRD-Stack:
    runs-on: self-hosted
    if: ${{ false }}
    steps:
      - uses: actions/checkout@v2
      - uses: chaplyk/docker-compose-remote-action@v1.1
        with:
          ssh_host: 10.0.0.107:22
          ssh_user: fink-home-docker-deploy
          ssh_key: ${{ secrets.NEW_DOCKER_SSH_KEY }}
          compose_file: adguard-dns-prd/docker-compose.yml
          pull: true
          build: true

  Deploy-Redmine-PRD-Stack:
    runs-on: self-hosted
    if: ${{ false }}
    steps:
      - uses: actions/checkout@v2
      - uses: chaplyk/docker-compose-remote-action@v1.1
        with:
          ssh_host: 10.0.0.107:22
          ssh_user: fink-home-docker-deploy
          ssh_key: ${{ secrets.NEW_DOCKER_SSH_KEY }}
          compose_file: redmine-prd/docker-compose.yml
          pull: true
          build: true